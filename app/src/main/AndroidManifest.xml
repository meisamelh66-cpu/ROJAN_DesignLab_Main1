<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ROJAN_DesignLab">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ROJAN_DesignLab"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait">
            <!--
                Final Premium Polish, Phase 3 finding: no screen in this app
                has a landscape-adapted layout (every screen is built as a
                vertical Column/LazyColumn), and nothing previously locked
                orientation. Rotating the device produced a genuinely broken
                render (overlapping text, large dead space) on the very
                first pilot screen tested — confirmed via real device
                testing, not theoretical. Locking portrait is the correct
                fix for an app with zero landscape-ready screens, not a
                workaround: it removes a state the UI was never designed to
                render, rather than papering over a real layout bug in that
                state.
            -->


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>