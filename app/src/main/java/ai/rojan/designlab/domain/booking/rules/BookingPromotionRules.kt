package ai.rojan.designlab.domain.booking.rules

/**
 * Future Promotion Rules — scaffolding only, per explicit instruction
 * not to invent business logic ahead of a real requirement.
 *
 * Intended future scope: coupon validity, promotion eligibility,
 * stacking rules. [ai.rojan.designlab.domain.booking.BookingState]
 * already has `promotionId`/`couponId` fields ready to be read by a
 * real implementation of this class later.
 * [ai.rojan.designlab.domain.booking.BookingEngine] does not reference
 * this class.
 */
class BookingPromotionRules
