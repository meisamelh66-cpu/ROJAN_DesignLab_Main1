package ai.rojan.designlab.domain.booking.rules

/**
 * Future AI Recommendation Rules — scaffolding only, per explicit
 * instruction not to invent business logic ahead of a real requirement.
 *
 * Intended future scope: recommending specialists, packages, promotions,
 * earlier appointment slots, higher-rated alternatives. No real AI/ML
 * logic exists anywhere in this codebase today (existing "AI" labels
 * elsewhere in the app are static cosmetic content, not live logic) —
 * fabricating a fake recommendation algorithm here would be inventing
 * exactly the kind of speculative business logic this refinement
 * explicitly rules out.
 * [ai.rojan.designlab.domain.booking.BookingEngine] does not reference
 * this class.
 */
class BookingAIRecommendationRules
