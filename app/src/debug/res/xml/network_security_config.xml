<?xml version="1.0" encoding="utf-8"?>
<!--
    Debug-build-only (this file lives under src/debug/, never merged into a
    release build) — permits plaintext HTTP to the local-dev backend hosts
    documented in data/remote/NetworkConfig.kt: 10.0.2.2 (the Android
    emulator's alias for the host machine) and localhost/127.0.0.1 (a
    physical device reaching the same host via `adb reverse`). Modern
    Android (targetSdk 28+) blocks cleartext traffic by default; there is
    no deployed HTTPS backend yet to test debug builds against (see
    build.gradle.kts's API_BASE_URL comment), so this is what lets local
    development/testing actually reach it. Release builds get no such
    config and stay cleartext-blocked everywhere, as they should once a
    real HTTPS backend URL is configured per build type.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
